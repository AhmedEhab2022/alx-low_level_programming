#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print all base 10 numbers
 *
 * Return: Always 0 (sucsses)
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0);
}
